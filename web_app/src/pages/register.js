import React from "react";
import Authenticate from "../components/authenticate";

const Register = () => {
  return (
    <div className="row">
      <Authenticate />
    </div>
  );
};

export default Register;
